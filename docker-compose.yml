services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 5432
      DB_NAME: fashion_catalog
      DB_USERNAME: postgres
      DB_PASSWORD: Toby2410$
