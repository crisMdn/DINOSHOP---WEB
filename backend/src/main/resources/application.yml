spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fashion_catalog
    username: postgres
    password: Toby123$
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: localhost
      port: 6379
  jackson:
    serialization:
      indent-output: true

server:
  port: 8080

app:
  notification:
    whatsapp-token: ${WHATSAPP_TOKEN:}
    whatsapp-phone: ${WHATSAPP_PHONE:}
    admin-email: ${ADMIN_EMAIL:admin@tienda.com}
